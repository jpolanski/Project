<form action="<?= url_for($actions['POST']['for'] . $actions['POST']['id']) ?>" method="POST">

<?php foreach($actions as $action) {?>
<?php extract($action);?>

    <?php if(!isset($method)) {?>
    <?= link_to(Presents::makeUrl($url,$values[$id]),$view)?>     
    <?php } else {?>
    
    <button class="btn btn-link"><?= $view ?></button>    
    <?php }?>


<?php }?>
</form>
